services:
  kavita:
    image: jvmilazz0/kavita:latest
    container_name: kavita
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - TZ=${TZ}
    volumes:
      - ./config:/kavita/config
      - /home/nyarlathotep/media/manga:/manga:ro
